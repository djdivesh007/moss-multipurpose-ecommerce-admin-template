image: node:10

pages:
  cache:
    paths:
    - node_modules/

  script:
  - npm i gulp -g
  - npm install
  - gulp build
  - mv dist public
  artifacts:
    paths:
    - public
  only:
    - master
