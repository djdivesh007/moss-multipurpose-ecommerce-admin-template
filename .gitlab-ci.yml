image: node:4.2.2

pages:
  cache:
    paths:
    - node_modules/

  script:
  - npm install -g gulp
  - npm install
  - gulp build
  - mv dist public
  artifacts:
    paths:
    - public
  only:
    - master
